// Phase 3 — Composite Patterns
//
// Higher-order compositions built from primitives:
//   AuthCard         — login/register card container with logo + title slots
//   FormField        — label + TextField + error text as a unit
//   FormSection      — grouped set of FormFields with optional heading
//   PlantCard        — plant thumbnail + name + watering status
//   ProfileHeader    — avatar + name + stats row
//   StatCard         — numeric stat with label
//   ActionRow        — icon + label + optional trailing element (settings-style)
//   FilterBar        — horizontal scroll of Chips
//   PageHeader       — screen title + optional subtitle + back button

export {};
